require 'spec_helper'

describe Ruboty::Shonin do
  it 'has a version number' do
    expect(Ruboty::Shonin::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
